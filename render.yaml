services:
  - type: web
    name: arkgpt-backend
    runtime: node
    rootDir: Backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 8080
      - key: OPENAI_API_KEY
        sync: false
      - key: MONGODB_URI
        sync: false